# This directory contains the leads backup file (leads_backup.jsonl).
# It is excluded from git via .gitignore because it contains real PII.
# The directory itself is tracked so it exists on fresh clones.
